# myatriumhealth-mcp needs no credentials: it relays requests through your
# signed-in my.atriumhealth.org tab via the fetchproxy bridge.

# fetchproxy concentrator port. The whole fetchproxy fleet shares 37149 — the
# Transporter extension dials that one port. Override only when hosting.
# MAH_WS_PORT=37149

# Bridge-less sign-in (optional). With these set, the server logs in directly and
# the browser bridge is not used at all. Verification stays human-in-the-loop: the
# portal sends a code to the account holder and you supply it via mah_verify_code.
# Leaving them unset keeps the default bridge mode, which holds no credentials.
# MAH_USERNAME=
# MAH_PASSWORD=

# Read from the environment first, then the first .env found among $MAH_DOTENV,
# ~/.myatriumhealth-mcp/.env and ./.env. Prefer the state-directory one: a client
# starts this server from its own working directory, so a .env in a checkout is
# usually not seen — and the fallback to the browser bridge is easy to miss.
# MAH_DOTENV=

# Where the session state is stored (0600). This file holds the LIVE SESSION
# COOKIE JAR as well as the portal's device-trust token, so treat it as a
# credential: anyone who can read it can act as the account until the session
# lapses. Defaults to ~/.myatriumhealth-mcp/device.json.
# MAH_DEVICE_FILE=
